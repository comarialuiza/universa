import styled from 'styled-components';


export const Container = styled.div`
    .day-header {
        color: var(--color-primary);
        font-weight: 300;
    }

    .month-title {
        color: var(--color-primary);
    }

    .year-title {
        color: var(--color-primary);
    }
`;